endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true

logging:
  level:
    root: INFO
    com.netflix.discovery: DEBUG

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.22.0.5:8748/eureka/
  instance:
    preferIpAddress: true
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
      
zipkin:
  storage:
    type: elasticsearch
    elasticsearch:
      hosts: http://40.125.201.3:29200/
      username: 123
      password: 456
